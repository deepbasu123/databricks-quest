[pytest]
# Default run = Tier 0 (pure / fake-DB, no Databricks). The integration tier
# is opt-in: QUEST_E2E=1 pytest -m integration tests/integration/
addopts = -m "not integration"
markers =
    integration: requires a real Databricks workspace (env-gated; see tests/integration/)
