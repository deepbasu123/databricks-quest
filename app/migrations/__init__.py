"""Lakebase schema migrations for Databricks Quest GameDay.

Migrations are plain ``.sql`` files named ``NNN_description.sql`` (ordered by the
numeric prefix). The runner in :mod:`run_migrations` applies any not yet recorded
in the ``schema_migrations`` table and is safe to run repeatedly.
"""
